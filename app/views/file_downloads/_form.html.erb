<%= form_for(@file_download) do |f| %>
  <% if @file_download.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@file_download.errors.count, "error") %> prohibited this file_download from being saved:</h2>

      <ul>
      <% @file_download.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
