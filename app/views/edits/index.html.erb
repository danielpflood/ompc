<h1>Listing edits</h1>

<table>
  <tr>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @edits.each do |edit| %>
  <tr>
    <td><%= edit.description %></td>
    <td><%= link_to 'Show', edit %></td>
    <td><%= link_to 'Edit', edit_edit_path(edit) %></td>
    <td><%= link_to 'Destroy', edit, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Edit', new_edit_path %>
